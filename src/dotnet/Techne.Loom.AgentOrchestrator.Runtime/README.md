# Techne.Loom.AgentOrchestrator.Runtime

RID-specific self-contained single-file runtime package for Loom Agent Execution Orchestrator.

The package contains one executable under `tools/<rid>/ao[.exe]` and does not require a preinstalled .NET runtime.
