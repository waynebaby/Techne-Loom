# Techne.Loom.SkillOrchestrator.Runtime

RID-specific self-contained single-file runtime package for Loom Skill Orchestrator.

The package contains one executable under `tools/<rid>/so[.exe]` and does not require a preinstalled .NET runtime.
