# Python Source Root

Reserved source root for the future Python package family.

Current status:
- no runnable Python runtime is committed yet
- public schemas and docs are being kept parallel with the .NET-first release
- add publishable PyPI packages under this root when the Python implementation slice starts
