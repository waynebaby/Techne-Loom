# Node.js Source Root

Reserved source root for the future Node.js package family.

Current status:
- no runnable Node.js runtime is committed yet
- public schemas and docs are being kept parallel with the .NET-first release
- add publishable npm packages under this root when the Node.js implementation slice starts
