# Local Offline Loom Skill Orchestrator Guide (Beta)

This is the offline Loom Skill Orchestrator guide entry for `/loom-skill-enhancement` beta channel.

Use these authoritative local repository docs when offline:

- `docs/en/reference/products/so-guide.md`
- `docs/en/reference/cli.md`
- `docs/en/reference/workflow-schema.md`

Execution authority reminder:

- In SO-exclusive governance mode, official skill runs are only `dotnet so.dll run` and `dotnet so.dll resume`.
