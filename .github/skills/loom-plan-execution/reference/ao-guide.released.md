# Local Offline AO Guide (Released)

This is the offline AO guide entry for `/loom-plan-execution` released channel.

Use these authoritative local repository docs when offline:
- `docs/en/reference/products/ao-guide.md`
- `docs/en/reference/cli.md`
- `docs/en/reference/workflow-schema.md`

Execution authority reminder:
- Official skill runs are only `dotnet ao.dll run` and `dotnet ao.dll resume`.
